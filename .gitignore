# Build directories
build/
target/
dist/
dist_*/
out/
node_modules/

# CMake
CMakeCache.txt
CMakeFiles/
CMakeScripts/
cmake_install.cmake
# CMake generated Makefiles (but not Sphinx Makefiles)
session_cpp/Makefile
session_cpp/build/Makefile
*.cmake

# C++ build artifacts
*.o
*.exe
*.dll
*.so
*.dylib
*.a
*.lib

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
env/
venv/
ENV/
env.bak/
venv.bak/
.env

# Rust
target/
**/target/
Cargo.lock
**/*.rs.bk

# IDE files
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Windows reserved names
nul
NUL
con
CON

# Claude temp files
tmpclaude-*

# Documentation output directories
docs_output/
_build/
html/

# Logs
*.log

# Project-specific build outputs
session_rust/target/
session_rust/docs_output/

session_py/__pycache__/
session_py/docs_output/
session_py/build/
session_py/dist/
session_py/*.egg-info/

session_cpp/build/
session_cpp/docs_output/

# OpenNURBS
opennurbs-8.x

# uvsession
# uvsession

# uv-managed per-project venvs (e.g. session_mcp/.venv created by `uv run --project session_mcp ...`)
.venv/

# Claude instructions
# nurbscurve.md
# protobuf.md


*.code-workspace

temp

# Build cache hashes
.py_install_hash
.api_index_hash

# Scratch/audit artifacts
__audit_*.txt

# Lesson narration audio (regenerated with edge-tts)
*.mp3

# Boolean-campaign trace dumps
validation/occt_trace/traces/
validation/v2_trace/traces/


# Test result JSONs are regenerated by every minitest run, so committed copies churn and can
# go stale. Worse, the class-summary guard counts any existing JSON as passing - a committed
# copy is a phantom pass for a class that did not run (the original reason pdf_test.json was
# untracked, now generalized). Fresh clones get them from the first minitest run; the viewer's
# testData.js is consolidated from the working-tree copies.
session_tests/session_*/*_test.json

# Test roundtrip scratch files written by minitest runs (CWD = repo root). Nothing reads the
# top-level copies - the viewer consolidates from session_{cpp,py,rust}/serialization/ instead -
# and instance_ref embeds a fresh random GUID every run, so a tracked copy is pure churn.
# Scoped to test_* so the committed schoring_*.pb data files stay tracked.
serialization/test_*

# Viewer .pb assets over GitHub's 50 MB recommendation (125 MB tops, over the 100 MB hard
# limit - rejected by the pre-receive hook). Kept on disk locally, never committed.
session_viewer/assets/pb/combined_scene.pb
session_viewer/assets/pb/draw_pd_treppenhaus04.pb
session_viewer/assets/pb/draw_pf_he.pb
session_viewer/assets/pb/draw_pi_laengsschnitt.pb
session_viewer/assets/pb/draw_pj_grundriss_og2.pb
session_viewer/assets/pb/draw_pj_treppenhaus_a.pb
