# All at once

Run in Git Bash / terminal:

```bash
./bash/minitest.sh                  # all 3 languages + viewer
./bash/minitest.sh --py --no-web    # one language (--py | --rust | --cpp)
./bash/quicktest.sh point --rust    # one class
```
