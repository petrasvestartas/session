# NN · Title of this step

<!--
HOW TO ADD AN INSTALL SECTION (it becomes an item in the left sidebar under the Install tab):

1. Copy this file to a new one named  NN-title.md  in this folder
   (session_tests/install_sections/). The NN number sets the order, e.g. 02-cpp.md, 03-python.md.
   The first "# heading" becomes the sidebar label.
2. Write your notes as normal Markdown.
3. Paste commands in fenced ```bash blocks (or rust/cpp/python/json/toml). Highlighted by Shiki.

Files starting with "_" (like this one) are ignored, so this template never shows up.
-->

## Section

```bash
echo "your commands here"
```
