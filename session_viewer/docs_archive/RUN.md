# Run the docs

```bash
cd session_viewer/docs
python serve.py
```

Then open http://localhost:8771 — the left sidebar lists every lesson.

Stop with `Ctrl+C`. No build step, no install; needs only Python 3.
