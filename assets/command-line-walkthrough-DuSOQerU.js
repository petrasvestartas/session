const e={title:"Use the command line",html:`<h1 id="use-the-command-line">Use the command line<a class="anchor" href="#/course/command-line-walkthrough#use-the-command-line" aria-label="Link to this section">#</a></h1>
<p>The black triangle at the top right returns to the viewer. The viewer uses a white command dock with black text. Older step captures below may still show the former toolbar and Run button; commands now run with Enter. To build this interface yourself, follow the course from <a href="#/course/22-runtime-helpers">lesson 22</a> to <a href="#/course/37-command-dock">lesson 37</a>.</p>
<h2 id="1-focus-and-type">1. Focus and type<a class="anchor" href="#/course/command-line-walkthrough#1-focus-and-type" aria-label="Link to this section">#</a></h2>
<p>The command area stays across the bottom and is ready for typing when the page opens. Enter a command first; its options appear only after you accept it with <strong>Enter</strong>, <strong>Tab</strong>, or <strong>Space</strong>. For example, type <code>Arctic</code>, press <strong>Enter</strong>, then click <strong>On</strong> or <strong>Off</strong> in the command row. Merely typing a matching prefix does not show options or change the shading.</p>
<p>Suggested command suffixes are selected inline. <strong>Up/Down</strong> or the mouse wheel browses the completion list; matching names come first and other commands remain available. <strong>Escape</strong> clears the pending command and returns focus to the scene. Complete commands such as <code>Arctic On</code> run directly with <strong>Enter</strong>.</p>
<p><img src="/session/docs/course/docs/screenshots/command-completion.png" alt="Current command dock with an inline completion and the command list" loading="lazy" decoding="async"></p>
<p>Click the white field, or focus the canvas and press <strong>:</strong> (colon), then type:</p>
<div class="code"><button class="copy" type="button" aria-label="Copy code">Copy</button><pre><code>line 0,130,0 100,130,0</code></pre></div>
<p>Coordinates are world-space <code>x,y,z</code>, separated by spaces between points. Do not type the <code>&gt;</code> history prefix.</p>
<p><img src="/session/docs/course/docs/screenshots/extensions-command-interface.png" alt="The bottom command area with the complete line command ready to run" loading="lazy" decoding="async"></p>
<p>Press <strong>Enter</strong>. The result appears above the field. Press <strong>Escape</strong> to return keyboard focus to the scene; press <strong>F</strong> to fit the scene if the line is outside the view.</p>
<p><img src="/session/docs/course/docs/screenshots/extensions-command-create.png" alt="The created line and command result" loading="lazy" decoding="async"></p>
<p>To create other objects, use the same sequence:</p>
<div class="code"><button class="copy" type="button" aria-label="Copy code">Copy</button><pre><code>point 0,0,0
polyline 0,180,0 100,180,0 100,230,0</code></pre></div>
<p>Drag the horizontal divider at the top of the command area up or down to show more or less history. The <strong>−</strong> button collapses history; <strong>+</strong> restores its previous height.</p>
<p>You can also type <code>Point</code>, <code>Line</code>, or <code>Polyline</code> and press <strong>Enter</strong>, or click the command in the suggestion list, then pick points in the scene. Point finishes after one click and Line after two. Polyline continues until <strong>Enter</strong> or <strong>Finish</strong>. You can mix clicks with coordinates, including relative coordinates such as <code>@10,0,0</code>. <strong>Escape</strong> cancels the unfinished drawing.</p>
<p>While Polyline is active, its submenu offers:</p>
<ul>
<li><strong>Points</strong>: the default sequence of clicked or typed vertices.</li>
<li><strong>Rectangle</strong>: pick two opposite corners on the construction plane.</li>
<li><strong>Polygon</strong>: pick a center and a radius point. The default is six sides; enter <code>Sides 5</code> before the radius point for a pentagon.</li>
</ul>
<p><code>Arctic On</code> enables the same soft contact shading; <code>Off</code> disables it. <strong>G</strong> toggles it when the viewport has keyboard focus. These controls preserve the camera. See <a href="#/course/ssao">Arctic quality and performance</a> for floor, plate and primitive examples.</p>
<p><code>Snap On</code> and <code>Snap Off</code> control endpoint, vertex and midpoint snapping, including during a draft. The default capture distance is 12 screen pixels. A blue marker names the active snap. <code>Layers On</code> and <code>Layers Off</code> show or hide the layer tree, which starts hidden. <code>Rotate</code> offers <strong>x</strong>, <strong>y</strong>, and <strong>z</strong>, followed by an angle.</p>
<h2 id="2-select-and-trim">2. Select and trim<a class="anchor" href="#/course/command-line-walkthrough#2-select-and-trim" aria-label="Link to this section">#</a></h2>
<p>Press <strong>Escape</strong> to return focus to the scene. Click the new line near its middle; the gumball confirms selection. Press <strong>:</strong> and type:</p>
<div class="code"><button class="copy" type="button" aria-label="Copy code">Copy</button><pre><code>trim 0.2 0.8</code></pre></div>
<p><img src="/session/docs/course/docs/screenshots/extensions-command-trim.png" alt="A selected line with its trim command ready" loading="lazy" decoding="async"></p>
<p>Press <strong>Enter</strong>. This keeps the middle 60% of the current line, from x=20 to x=80. Line and NURBS parameters are normalized to the current curve: this command does not trim against another object.</p>
<h2 id="3-extend-the-result">3. Extend the result<a class="anchor" href="#/course/command-line-walkthrough#3-extend-the-result" aria-label="Link to this section">#</a></h2>
<p>Press <strong>Escape</strong> and select the trimmed line again. Run:</p>
<div class="code"><button class="copy" type="button" aria-label="Copy code">Copy</button><pre><code>extend -0.2 1.2</code></pre></div>
<p><img src="/session/docs/course/docs/screenshots/extensions-command-extend.png" alt="The extended line and command feedback" loading="lazy" decoding="async"></p>
<p>The interval applies to the trimmed line, so its endpoints become x=8 and x=92. It does not restore the original endpoints.</p>
<h2 id="4-explode-and-undo">4. Explode and undo<a class="anchor" href="#/course/command-line-walkthrough#4-explode-and-undo" aria-label="Link to this section">#</a></h2>
<p>Create the polyline from step 1. Press <strong>Escape</strong>, then <strong>F</strong>, and click its horizontal segment. Click the bottom command field and run:</p>
<div class="code"><button class="copy" type="button" aria-label="Copy code">Copy</button><pre><code>explode</code></pre></div>
<p><img src="/session/docs/course/docs/screenshots/extensions-command-explode.png" alt="The polyline replaced by two lines, with the command history visible" loading="lazy" decoding="async"></p>
<p>Two independent lines replace the selected polyline. Run <code>undo</code> to restore it; <code>redo</code> repeats the explosion. Explode supports polylines, not meshes or BReps.</p>
<p><img src="/session/docs/course/docs/screenshots/extensions-command-undo.png" alt="Undo restores the polyline" loading="lazy" decoding="async"></p>
<h2 id="selection-and-keyboard-focus">Selection and keyboard focus<a class="anchor" href="#/course/command-line-walkthrough#selection-and-keyboard-focus" aria-label="Link to this section">#</a></h2>
<p><strong>L</strong> in the scene toggles the right-hand Layers panel. You can also run <code>Layers</code> and choose <strong>On</strong> or <strong>Off</strong>. Expand a group with its small triangle; click its name to select descendants or its bulb to hide them. The command field owns letters while you type. <strong>Escape</strong> returns focus to the scene; the bottom command area remains visible. A failed command displays its reason and preserves the document.</p>
<p>On a phone, tap the command field: the keyboard opens and what you type goes into the field, with <strong>Enter</strong> running the line. <strong>Enter</strong> on an empty field closes the command box but keeps the keyboard up, and then the letters are the scene&#39;s own keys - <strong>H</strong> hides, <strong>S</strong> shows all, <strong>F</strong> fits, <strong>P</strong> toggles x-ray, <strong>1</strong>-<strong>7</strong> set the views - exactly as on a desktop; <strong>:</strong> reopens the field. Tapping the scene puts the keyboard away.</p>
<p><code>Opacity 0..1</code> sets how solid the shaded faces are: <code>Opacity 1</code> is opaque, <code>Opacity 0</code> is x-ray with only the edges left, and anything between is glass - the faces dim without hiding the linework inside or behind them. A document with elements opens at 0.7 so their features show through; the <code>Opacity</code> command or <code>?opacity=</code> in the address bar overrides that.</p>
<p>The captions identify the browser, GPU configuration and exact operations.</p>
<h2 id="selection-commands-touch-editing-and-saving">Selection commands, touch editing and saving<a class="anchor" href="#/course/command-line-walkthrough#selection-commands-touch-editing-and-saving" aria-label="Link to this section">#</a></h2>
<p>Use <code>Object</code>, <code>Controls</code>, <code>Edge</code>, and <code>Face</code> to choose selection tools. Select an object before <code>Controls</code>, then select its original vertex or NURBS control. <code>Edge</code> and <code>Face</code> let you tap source subobjects directly. Shift-click adds objects to a shared selection and gumball; selecting a layer group selects its descendants. Drag a gumball handle with a mouse or one finger, or enter <code>Move</code>, <code>Rotate</code>, or <code>Scale</code>. A second finger cancels an active edit. <code>Layers On</code> opens the right panel when needed.</p>
<p><strong>Save</strong> downloads one <code>.session</code> file containing all retained documents, updated source geometry, placements, hidden objects, selection locks, display colors and annotations. <strong>Open</strong> restores it. Partially streamed scenes cannot yet be saved as complete files. Meshes, NURBS boundaries and compatible joined BRep edits are supported; BRep changes requiring trim reconstruction return an error and preserve the original shape.</p>
<p><a href="#/course/29-docked-workspace">Checkpoint 8</a> explains the implementation and shows how to add a toolbar button by extending the <code>TOOLBAR</code> table.</p>
<h2 id="layer-visibility-selection-locks-and-colors">Layer visibility, selection locks and colors<a class="anchor" href="#/course/command-line-walkthrough#layer-visibility-selection-locks-and-colors" aria-label="Link to this section">#</a></h2>
<p>Expand the right-hand tree to see objects and their children. Click a bulb to hide or show that subtree, a lock to prevent or allow selection, and a color swatch to choose a palette color or edit its RGB values. Save/Open retains all three settings. <a href="#/course/30-layer-tree">Checkpoint 9</a> implements these controls and the live shell preview.</p>
<h2 id="split-with-cutter-curves">Split with cutter curves<a class="anchor" href="#/course/command-line-walkthrough#split-with-cutter-curves" aria-label="Link to this section">#</a></h2>
<p>Create a <code>Line</code>, <code>Polyline</code>, or <code>Curve</code> using world coordinates; <code>Curve</code> uses the supplied points as NURBS control points, with degree up to three. Created curves use a screen-space pen so they remain visible when zoomed out.</p>
<p>Select a target curve, standalone surface, or one BRep face. Use <strong>Ctrl+Shift</strong> or the <code>Face</code> command for face selection. Press <strong>Split</strong>, select one or more cutter curves in the viewport or layer tree, then press <strong>Enter</strong> or tap <strong>Split</strong> again. <strong>Esc</strong> cancels. The command dock reports the selected cutter count and explains invalid input.</p>
<p>Curve splits keep all pieces. Face splits preserve existing holes and retain every region inside the owning BRep, updating shared boundaries so the shell stays joined. Cutters must intersect curves in 3D or lie on the selected surface within tolerance. Ambiguous overlaps and unsupported seam cases are rejected. This operation does not perform booleans, project cutters, cap holes or divide a solid into separate volumes.</p>
<p>Use <strong>Undo</strong>, <strong>Redo</strong>, and <strong>Save/Open</strong> to verify the retained source. <a href="#/course/31-splitting">Checkpoint 10</a> includes the kernel implementation and viewer wiring.</p>
<h2 id="expected-viewer-result">Expected viewer result<a class="anchor" href="#/course/command-line-walkthrough#expected-viewer-result" aria-label="Link to this section">#</a></h2>
<p>Click a layer swatch to change its display colors. Surface objects expose <strong>Faces</strong> and <strong>Edges</strong> independently. <strong>Original</strong> restores the authored colors for the active channel; parent controls apply to children. Save/Open retains both overrides.</p>
<p>The current viewer has a command dock across the bottom and an optional right-hand layer tree with bulbs, selection locks and color swatches. Selection tools are commands; the older capture below still includes the former left toolbar. Split keeps both face regions in the joined shell. The selected region has its gumball; Save/Open retains the edited geometry and layer settings. See the <a href="/session/docs/course/docs/screenshots/extensions-workspace-current-phone.png">phone layout</a>.</p>
<p><a href="/session/docs/course/docs/screenshots/extensions-workspace-current-desktop.png"><img src="/session/docs/course/docs/screenshots/extensions-workspace-current-desktop.png" alt="Full viewer result for command line walkthrough" loading="lazy" decoding="async"></a></p>
`,toc:[{level:2,id:"1-focus-and-type",text:"1. Focus and type"},{level:2,id:"2-select-and-trim",text:"2. Select and trim"},{level:2,id:"3-extend-the-result",text:"3. Extend the result"},{level:2,id:"4-explode-and-undo",text:"4. Explode and undo"},{level:2,id:"selection-and-keyboard-focus",text:"Selection and keyboard focus"},{level:2,id:"selection-commands-touch-editing-and-saving",text:"Selection commands, touch editing and saving"},{level:2,id:"layer-visibility-selection-locks-and-colors",text:"Layer visibility, selection locks and colors"},{level:2,id:"split-with-cutter-curves",text:"Split with cutter curves"},{level:2,id:"expected-viewer-result",text:"Expected viewer result"}]};export{e as default};
